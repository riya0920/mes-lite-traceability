# SE-2 extensions — generated by `extend.py`, not hand-edited

## 1. The lot-tracked model, actually exercised

The first build *defined* a lot-tracked product and never ran one, so the dual serial/lot model was designed and not demonstrated. `PLATE-200` now runs as a batch of 400 with 25 plates scrapped **within** the batch at drilling.

| work order | op | started | completed | scrapped | in process |
|---|---|---|---|---|---|
| WO-2001 | 10 | 400 | 400 | 0 | 0 |
| WO-2001 | 20 | 400 | 375 | 25 | 0 |
| WO-2001 | 30 | 400 | 375 | 0 | 25 |

0 conservation violations. **Quantity conservation works on quantities, not on unit counts** — which is what makes one ledger serve both models: a serialised unit is simply a batch of one.

**The traceability cost, which is the part worth understanding.** A serialised product resolves a recall to a specific object with a specific history. A lot-tracked product resolves it to *some quantity within a batch* — here, batch of 400 (one unit row). That is not a modelling shortcut, it is physical: if 400 plates are sheared from one coil and mixed in a tote, no data model can say which plate came from which end. **Serialise or not is a product-design decision with a traceability price**, and it is the trade that gets made at design and regretted during a recall.

## 2. Property-based testing of the rework state machine

The spec asks for this explicitly, and the first build offered four deterministic rework events — coverage, not proof. Here **200 randomly generated routing histories** with 0–3 rework loops each, re-entering at randomly chosen earlier operations, are checked against invariants that must hold for *every* history:

| property | holds |
|---|---|
| P1 — quantity conservation at every operation | **0 violations** across 5 (order, op) pairs |
| P2 — precedence never violated | see failures below |
| P3 — completions never exceed starts at any operation | see failures below |
| P4 — every rework entry has a REWORK-dispositioned NCR | see failures below |

**200/200 histories built successfully, generating 294 rework entries. Property failures: 0.**


**P3 is the one that earns its place.** Without the "most recent rework entry" comparison in `complete_operation`, a reworked unit can complete an operation twice for a single pass — and the work-order ledger still balances, because the extra completion is offset elsewhere. The violation is only visible per unit per operation, which is exactly the granularity a hand-written test tends not to check and a property test does by construction.

## 3. The dispatch list

Not planning — that is ERP at ISA-95 level 4. This is the **dispatch list**: given what is in process right now, what runs next at each work centre and how much work is queued there.

| work centre | capacity | units queued | queued work (h) | hours per resource | next up |
|---|---|---|---|---|---|
| WC-WELD | 2 | 5 | 0.25 | **0.12** | WO-4001-U000, WO-4001-U004, WO-4001-U008 |
| WC-MACH | 3 | 5 | 0.33 | **0.11** | WO-4001-U001, WO-4001-U005, WO-4001-U009 |
| WC-INSP | 2 | 4 | 0.13 | **0.07** | WO-4001-U003, WO-4001-U007, WO-4001-U011 |
| WC-PAINT | 1 | 4 | 0.07 | **0.07** | WO-4001-U002, WO-4001-U006, WO-4001-U010 |

**WC-WELD carries the most load per resource** (0.12 h). Sorting by *hours per resource* rather than by unit count is the whole point — a work centre with three machines and twelve units queued is in better shape than one with a single machine and five.

**It is derived entirely from the execution ledger**, with no separate scheduling state. That is deliberate: a dispatch list held in its own table drifts out of sync with what actually happened on the floor, and then the operators stop trusting it and go back to the whiteboard. Deriving it means it cannot disagree with the record.

**Still not built:** finite-capacity scheduling, due-date sequencing, setup matrices, or any notion of when a work centre becomes free. This is a priority list, not a schedule.

---
*Regenerate with `python extend.py`.*
